apiVersion: v1
kind: Service
metadata:
  name: product-details-mfe
spec:
  type: {{ .Values.service.type }}
  selector:
    app: product-details-mfe
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
