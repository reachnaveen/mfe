apiVersion: v1
kind: Service
metadata:
  name: product-search-mfe
spec:
  type: {{ .Values.service.type }}
  selector:
    app: product-search-mfe
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
